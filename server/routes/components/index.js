const { Memories } = require('./Memories');
const { Images } = require('./Images');
const { LocationDetails } = require('./LocationDetails');
const { Locations } = require('./Locations');

const routeComponents = {
  Memories,
  Images,
  LocationDetails,
  Locations,
};

module.exports = { routeComponents };
